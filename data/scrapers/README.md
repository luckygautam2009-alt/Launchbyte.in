# Scrapers

One folder per opportunity category. Each should produce raw output
consistent enough for `pipelines/` to normalize.

Status: Planned. Do not implement aggressive scraping before the
opportunity schema is finalized — see `docs/database/schema.md`.
