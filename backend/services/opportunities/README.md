# Opportunities Service

Business logic for listing, searching, filtering, and retrieving
opportunities. Will eventually call into `ai/recommendation/` for
personalized ordering and `data/opportunity-data/normalized/` (via the
database) as its source of truth. Not implemented yet.
