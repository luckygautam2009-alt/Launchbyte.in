# Utils

Small, reusable, dependency-light helper functions shared across backend
services (e.g. slugify, date formatting, pagination helpers).

Keep this folder a last resort — most logic belongs in a specific service.
Nothing here yet.
