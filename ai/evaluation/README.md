# Evaluation

Eval harnesses for AI quality — e.g. recommendation relevance, duplicate
detection precision/recall, copilot response quality.

Status: Planned. Build alongside the first real AI feature, not before —
an eval harness needs something to evaluate.
