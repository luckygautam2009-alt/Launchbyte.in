# Hooks

Shared React hooks used across more than one feature (e.g. `useDebounce`,
`useAuth` once auth exists). Feature-specific hooks should live inside that
feature's folder instead. Nothing implemented yet.
