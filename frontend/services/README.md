# Services

Frontend-side API service functions — thin wrappers around calls to the
backend (e.g. `getOpportunities()`, `login()`). Keep network logic out of
components; call these instead. Nothing implemented yet.
